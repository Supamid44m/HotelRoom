version: '3.8'

services:
#  app:
#    image: 'docker-spring-boot-postgres:latest'
#    build:
#      context: .
#    container_name: app
#    depends_on:
#      - db
#    environment:
#      - POSTGRES_USER=root
#      - POSTGRES_PASSWORD=1234
#      - SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/hotelRoom
#      - SPRING_DATASOURCE_USERNAME=root
#      - SPRING_DATASOURCE_PASSWORD=1234
#      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
#
#    ports:
#      - "8080:8080"

  db:
    image: postgres
    container_name: db
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=1234
      - POSTGRES_DB=hotelRoom
    ports:
      - "5432:5432"